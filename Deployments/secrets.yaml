apiVersion: v1
kind: Secret
metadata:
  name: database-secret
  namespace: default  # Change as needed
  annotations:
    avp.kubernetes.io/path: "creds/data/db-creds"
type: Opaque
stringData:
  username: <username>
  password: <password>
