apiVersion: v1
kind: Secret
metadata:
  name: database-secret
  namespace: default  # Change as needed
  annotations:
    avp.kubernetes.io/path: "creds/data/naren"
type: Opaque
stringData:
  db_username: <username>
  db_password: <password>
